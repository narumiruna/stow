# Stow v0.1 Remaining Release Checklist

The iOS signing and TestFlight infrastructure blockers are resolved. Build `0.1.1 (1)` passed App Store Connect processing and was installed from TestFlight on 2026-09-19.

## Completed infrastructure

- Paid individual Apple Developer Program team `A4YQL6FFTK` is available.
- The team authorizes:
  - Multiplatform host `dev.narumi.stow`
  - iOS/iPadOS extension `dev.narumi.stow.share-ios`
  - App Group `group.dev.narumi.stow`
  - CloudKit container `iCloud.dev.narumi.stow`
- Automatic signing generated development and App Store profiles for both iOS bundles with their committed capabilities.
- The App Store Connect app record exists, and an internal TestFlight install succeeds.

## Remaining App Store release actions

1. Run the remaining signed online/offline/conflict/reinstall/attachment round trips in `v0.1-test-matrix.md` on two devices using the same test iCloud account.
2. Verify the CloudKit development schema and deploy it to production.
3. Register and provision corrected macOS extension `dev.narumi.stow.share-macos`, then validate a signed macOS archive.
4. Publish reviewed support/privacy URLs, complete App Store Connect metadata and final screenshots, and resolve every remaining row in the release matrix.

## Current provisioning evidence

- iOS build `0.1.1 (1)` passed archive, upload, processing, and TestFlight installation.
- Development and App Store profiles exist for `dev.narumi.stow` and `dev.narumi.stow.share-ios`.
- CloudKit production deployment and macOS distribution validation are not yet recorded as complete.
