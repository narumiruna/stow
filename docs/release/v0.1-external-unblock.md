# Stow v0.1 External Release Unblock Checklist

The repository implementation and owner-approved automated scope pass. The remaining plan gates require Apple Developer infrastructure unavailable to this checkout.

## Required owner actions

1. Select a paid Apple Developer Program team that permits App Groups, iCloud/CloudKit, macOS provisioning, and App Store distribution.
2. Create or authorize these identifiers on that team:
   - `app.stow.Stow`
   - `app.stow.Stow.ShareExtension`
   - `app.stow.Stow-macOS`
   - `app.stow.Stow-macOS.ShareExtension`
   - App Group `group.app.stow.Stow`
   - CloudKit container `iCloud.app.stow.Stow`
3. Create development/distribution profiles containing the committed capabilities, then select the team locally without committing credentials.
4. Sign current iPhone and Mac builds into the same test iCloud account and run the remaining online/offline/conflict/reinstall/attachment round trips in `v0.1-test-matrix.md`.
5. Deploy the verified CloudKit schema to production.
6. Publish reviewed support/privacy URLs, configure App Store Connect metadata, upload signed archives, and complete Xcode/App Store validation.

## Current provisioning evidence

- Available identity: personal team certificate for team `A4YQL6FFTK`.
- iOS automatic provisioning generated profiles, but Xcode rejected them because they do not contain the App Group and iCloud container entitlements.
- macOS archive provisioning failed because the team could not generate matching Mac development profiles.
- Ad-hoc copied products pass exact entitlement extraction checks, but ad-hoc signatures cannot grant CloudKit/App Group capabilities or validate an App Store archive.
